#!/usr/bin/env python

def options(opt):
    opt.load('waf_unit_test')

def configure(cfg):
    cfg.load('waf_unit_test')

def build(bld):
    bld.program(
        features     = 'test',
        target       = 'tests',
        source       = ['tests.vala'] + bld.path.ant_glob('*-test.vala'),
        use          = 'valum',
        vapi_dirs    = '../vapi',
        install_path = None)
