vsgi_loader = executable('vsgi-loader-@0@'.format(api_version), 'vsgi-loader.vala',
                         dependencies: [glib, gobject, gio, gmodule, soup, vsgi],
                         install: true)
