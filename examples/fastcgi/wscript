#!/usr/bin/env python

def configure(cfg):
    pass

def build(bld):
    bld.program(
        target       = 'fastcgi',
        use          = 'valum',
        source       = ['app.vala'],
        vapi_dirs    = '../../vapi',
        install_path = None)

