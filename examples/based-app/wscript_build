#!/usr/bin/env python

bld.program(
    package   = ['glib-2.0'],
    target    = 'app',
    use       = 'valum',
    source    = ['app.vala'],
    uselib    = ['GLIB', 'SOUP', 'GEE', 'CTPL'],
    vapi_dirs = ['../../vapi'])
