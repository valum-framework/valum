#!/usr/bin/env python

bld.program(
    packages     = ['glib-2.0'],
    target       = 'cgi-bin/app.cgi',
    use          = 'valum',
    source       = ['app.vala'],
    uselib       = ['GLIB', 'SOUP', 'GEE', 'FCGI', 'GCOV'],
    vapi_dirs    = ['../../vapi'],
    install_path = None)
